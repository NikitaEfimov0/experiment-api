# Local run configuration — Pi agent live on the LAN (pi002)
# Loaded via: npm run start:local

# --- Database (adjust to your local Postgres) ---
DATABASE_URL=еьз
PGSSL=require

# --- Raspberry Pi recording agent ---
PI_AGENT_URL=http://192.168.0.130:8090
INGEST_TOKEN=17b8d43a0912a84b48a683236afcca62f9118fac7d3bc96e

# --- Storage & port (defaults) ---
DATA_DIR=./data
PORT=3000
# Defaults already fit the Pi's timing; uncomment only to change.
# PI_START_TIMEOUT_MS=20000
# PI_STOP_TIMEOUT_MS=60000
